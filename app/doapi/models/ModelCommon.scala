package doapi.models

object ModelCommon {

  
  object state {
    val name = "state"

    val v_ready = "ready"
    val v_downloaded = "downloaded"
    val v_processing = "processing"
    val v_failed = "failed"
    
  }
}